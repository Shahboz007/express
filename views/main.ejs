<%- include('includes/head.ejs')%> <%- include('includes/navbar.ejs')%>

<main>
  <h1>Users list</h1>
  <% if (users.length > 0) {%> <% for (let user of users) {%>
  <div class="card">
    <img
      src="https://www.w3schools.com/howto/img_avatar.png"
      alt="Avatar"
      style="width: 100%"
    />
    <div class="container">
      <h4>
        <b>Name: <%=user.username%></b>
      </h4>
      <p>Age: <%=user.age%></p>
    </div>
  </div>
  <%}%> <% } else {%>
  <p>Userlarni barcha listi...</p>
  <% } %>
</main>

<%- include('includes/food.ejs') %>
